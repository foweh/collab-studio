version: '3.8'

services:
  collab-studio:
    build: .
    ports:
      - "3000:3000"
      - "41234:41234/udp"
    environment:
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-}
      - PORT=3000
    volumes:
      - ./data:/app/data
    restart: unless-stopped
